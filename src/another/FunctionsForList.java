package another;

public class FunctionsForList {

	/*	list size: size()
		Get the element from a position: get(position)
		Insert element in list: add(obj), add(int, obj)
		Remove elements from list: remove(obj), remove(int), removeIf(Predicate)
		find element position: indexOf(obj), lastIndexOf(obj)
		
		Filter list based on predicate:
		List<Integer> result = list.stream().filter(x -> x > 4).collect(Collectors.toList());
		
		Find first occurrence based on predicate:
		Integer result = list.stream().filter(x -> x > 4).findFirst().orElse(null);
			 
	 */
}
